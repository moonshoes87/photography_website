
<h1>Photography Website</h1>


<div id="slideshow">
  <% @photos.each do |p| %>
  <div>
    <%= link_to(image_tag(p.url), { controller: "photo", action: "show", id: "#{p.id}" }, class: "crop slideshow") %>
  </div>
  <% end %>
</div>

