<h1>Photo#show</h1>
<p>Find me in app/views/photo/show.html.erb</p>
<div>
  <title><%= @photo.title %></title>
  <div>
    <div class="image main"><%= image_tag "#{@photo.url}" %></div>
    <span class="description"><%= @photo.description %></span>
  </div>
   <p><%= link_to "Back", photo_index_path %></p>
   <p>You will need to fix the Back, so it goes to where the user actually was.  Also, please make the css nice.</p>
</div>
